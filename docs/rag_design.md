# RAG Design

The RAG layer loads local traffic knowledge base markdown files, chunks by headings and paragraphs, and retrieves with TF-IDF. It returns chunks, scores, citations and `no_hit`.

Knowledge areas include operation report templates, congestion diagnosis, incident response, signal control principles and digital twin traffic platforms.
